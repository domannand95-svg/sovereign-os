[package]
name = "sovereign-audit"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
blake3 = { workspace = true }
sovereign-registry = { version = "0.1.0", path = "../sovereign-registry" }
