[package]
name = "sovereign-registry"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
blake3.workspace = true
sovereign-ledger = { version = "0.1.0", path = "../sovereign-ledger" }
