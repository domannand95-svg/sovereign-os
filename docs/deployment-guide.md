# Deployment Guide

## Overview

This guide describes how to deploy and operate Sovereign OS in local and clustered environments.

## Prerequisites

- Rust toolchain installed
- Repository cloned locally
- Workspace dependencies available
- Network access between cluster nodes

## Build

From the repository root:

```bash
cargo build --workspace